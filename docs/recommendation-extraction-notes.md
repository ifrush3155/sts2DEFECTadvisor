# 推荐图整理记录

## 输出文件

正式推荐数据已写入：

```text
data/recommendations/slay-the-spire-2-manual.json
```

数据来源：

- 推荐顺序：`data/references/card-ranking-source.png`
- 卡牌 ID 和中英文名称：Spire Codex Defect cards API

## 分组数量

| 分组 | 条目数 |
| --- | ---: |
| 过牌端口 | 6 |
| 费用端口（0 费默认属于此端口） | 5 |
| 伤害端口 | 10 |
| 伤害端口（AOE） | 9 |
| 防御端口（伤害复合型） | 7 |
| 防御端口 | 9 |
| 上限端口 | 27 |
| 启动端口（防御端口默认属于此端口） | 4 |
| 稳定端口 | 7 |

合计 84 条端口条目。注意：同一张卡可以出现在多个端口，因此条目数不等于唯一卡牌数。

## 已确认项

以下条目曾因图片清晰度较低列为待确认，现已由用户确认：

| 分组 | 位置 | 已录入 ID | 已录入英文名 | 需要确认的原因 |
| --- | ---: | --- | --- | --- |
| 稳定端口 | 1/7 | `FLAK_CANNON` | Flak Cannon | 已确认。 |

## 已处理的重复卡

推荐图中存在同一张牌出现在多个端口的情况。当前校验器已调整为允许重复卡牌，并新增 `lookup_all()` 支持查询同一张牌的多个端口。

已观察到的重复示例：

- `ROCKET_PUNCH`：过牌端口、伤害端口。
- `ALL_FOR_ONE`：上限端口、稳定端口。
